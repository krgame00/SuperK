@AGENTS.md

## Agent skills

### Issue tracker

Issues and specs are tracked as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

This repository uses a single domain context at `CONTEXT.md` with architectural decisions under `docs/adr/`. See `docs/agents/domain.md`.
